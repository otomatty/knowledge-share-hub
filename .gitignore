# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
coverage
test-results
playwright-report
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel

supabase/.temp

# Package managers: this project uses Bun (bun.lock). Reject other lock
# files so dependency resolution stays consistent between local, CI, and
# Vercel (vercel.json uses `bun install`).
package-lock.json
yarn.lock
pnpm-lock.yaml